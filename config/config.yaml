artifacts_root: artifacts

dataset:
  name: EdinburghNLP/xsum
  cache_dir: artifacts/data/cache
  processed_dir: artifacts/data/processed

models:
  primary: google/flan-t5-base
  efficient_baseline: sshleifer/distilbart-cnn-6-6
  reference: facebook/bart-large-cnn

model_artifacts:
  root_dir: artifacts/models
  baseline_dir: artifacts/models/baselines
  fine_tuned_dir: artifacts/models/flan-t5-base-lora

results:
  root_dir: results
  predictions_dir: results/predictions
  metrics_file: results/model_comparison.json

logs:
  root_dir: logs
